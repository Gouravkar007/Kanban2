{
  "name": "kanban-project-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "test": "npm --prefix frontend test",
    "test:e2e": "npm --prefix frontend run test:e2e"
  }
}
